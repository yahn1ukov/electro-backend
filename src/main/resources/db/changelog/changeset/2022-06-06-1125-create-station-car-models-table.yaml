databaseChangeLog:
  - changeSet:
      id: create-station-car-models-table
      author: Yahniukov Andrii
      changes:
        - createTable:
            tableName: carModels
            columns:
              - column:
                  name: id
                  type: bigint
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: model
                  type: varchar(50)
                  constraints:
                    nullable: false
              - column:
                  name: station_id
                  type: bigint
                  constraints:
                    foreignKeyName: fk_station_model
                    references: stations(id)
                    nullable: false
                    deleteCascade: true